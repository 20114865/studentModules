@model IEnumerable<studentModules.Models.study>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HoursSpecificModule)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TodayDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HoursSpecificModule)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TodayDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ModuleCode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ModuleCode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ModuleCode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
